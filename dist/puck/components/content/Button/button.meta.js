export const buttonMeta = {
    name: 'Button',
    label: 'Button',
    description: 'CTA button rendered as <a href> with hover effects. 5 colors (bg / text / border / hover-bg / hover-text) accept hex or theme tokens; resolveColor() resolves tokens via var(--theme-*) CSS variables. Variant (primary/secondary/outline/ghost/danger) toggles border treatment; size scales padding+font; alignment controls flexbox justify.',
    category: 'content',
    intent: ['button', 'cta', 'call-to-action', 'action', 'link', 'submit'],
    visualRole: 'inline',
    dataDeps: [],
    copyFields: ['text', 'url'],
    themeable: ['backgroundColor', 'textColor', 'borderColor', 'hoverBackgroundColor', 'hoverTextColor'],
    a11yRisk: 'medium',
    a11yNotes: 'Renders as <a href> — if the destination is not a real URL, use # sparingly. Confirm target=_blank triggers rel=noopener noreferrer automatically. Color contrast must meet WCAG AA against the background. Hover-only color changes fail accessibility — provide a non-color affordance (shadow change, scale).',
    mobileBehavior: 'responsive',
    searchTags: ['button', 'cta', 'action', 'a', 'href', 'click', 'submit'],
    props: {
        text: { type: 'string', required: true },
        url: { type: 'string', required: true },
        openInNewTab: { type: 'boolean', required: true },
        variant: { type: 'enum', options: ['primary', 'secondary', 'outline', 'ghost', 'danger'] },
        size: { type: 'enum', options: ['sm', 'md', 'lg', 'xl'] },
        fullWidth: { type: 'boolean', required: true },
        backgroundColor: { type: 'string' },
        textColor: { type: 'string' },
        borderColor: { type: 'string' },
        hoverBackgroundColor: { type: 'string' },
        hoverTextColor: { type: 'string' },
        showIcon: { type: 'boolean', required: true },
        iconPosition: { type: 'enum', options: ['left', 'right'] },
        borderRadius: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'full'] },
        shadow: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'xl'] },
        alignment: { type: 'enum', options: ['left', 'center', 'right'] },
        marginTop: { type: 'number', required: true },
        marginBottom: { type: 'number', required: true },
    },
};
//# sourceMappingURL=button.meta.js.map
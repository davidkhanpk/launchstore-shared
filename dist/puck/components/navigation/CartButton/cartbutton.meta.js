export const cartButtonMeta = {
    name: 'CartButton',
    label: 'Cart Button',
    description: 'Cart button with item-count badge (auto-syncs via the consumer\'s CartContext). The shared component is cart-library-agnostic: consumer wrapper passes cartCount (number) and onOpenCart (() => void) at render-time. Stores that want this badge to update reactively in the Puck-rendered storefront must implement D-3 (shared CartContext) or inject cartCount from their own provider.',
    category: 'navigation',
    intent: ['cart', 'basket', 'nav-cart'],
    visualRole: 'inline',
    dataDeps: ['cartCount (injected)', 'onOpenCart (injected)'],
    copyFields: [],
    themeable: ['iconColor', 'hoverColor', 'badgeBackgroundColor', 'badgeTextColor'],
    a11yRisk: 'low',
    a11yNotes: 'aria-label includes item count. Button is keyboard-focusable (native <button>).',
    mobileBehavior: 'responsive',
    searchTags: ['cart', 'basket', 'nav-cart', 'shopping-cart'],
    props: {
        showLabel: { type: 'boolean', required: true },
        label: { type: 'string', required: true },
        showBadge: { type: 'boolean', required: true },
        badgePosition: { type: 'enum', options: ['top-right', 'top-left', 'bottom-right'], required: true },
        iconSize: { type: 'enum', options: ['sm', 'md', 'lg'], required: true },
        iconColor: { type: 'string', required: true },
        hoverColor: { type: 'string', required: true },
        badgeBackgroundColor: { type: 'string', required: true },
        badgeTextColor: { type: 'string', required: true },
        style: { type: 'enum', options: ['minimal', 'outlined', 'filled'], required: true },
        cartCount: { type: 'number', required: false },
    },
};
//# sourceMappingURL=cartbutton.meta.js.map
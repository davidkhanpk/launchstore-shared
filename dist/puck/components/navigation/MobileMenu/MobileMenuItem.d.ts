import React from 'react';
import type { MobileMenuItemProps } from './mobilemenu.types';
/**
 * MobileMenuItem — single menu row with optional accordion for
 * children OR mega-menu subcategories. Stateless re: open/closed
 * (each row manages its own state).
 */
export declare const MobileMenuItem: React.FC<MobileMenuItemProps>;
export default MobileMenuItem;
//# sourceMappingURL=MobileMenuItem.d.ts.map
export const productCardMeta = {
    name: 'ProductCard',
    label: 'Product Card',
    description: 'Single-product card with image / title / price / badges / add-to-cart. Stand-in product card for stores that don\'t ship a ProductPreview component. Optional renderProduct escape hatch: storefront passes its own renderer (HttpTypes.StoreProduct \u2192 themed Card) via the consumer wrapper. Default card renders a full feature set using only Tailwind classes \u2014 useful for editor preview and frontend (no Medusa SDK).',
    category: 'product',
    intent: ['product', 'card', 'item', 'preview-card', 'card-grid'],
    visualRole: 'inline',
    dataDeps: ['product (injected or stub)', 'renderProduct (optional)'],
    copyFields: ['buttonText'],
    themeable: ['priceColor', 'cardBackground', 'accentColor', 'cardRadius', 'cardBorder'],
    a11yRisk: 'low',
    a11yNotes: 'Image has alt from product.title. Add-to-cart uses real <button>. Badges are decorative <span>s (live region not needed \u2014 sale/new/low-stock already implied visually for screen readers via aria-label on the action button).',
    mobileBehavior: 'responsive',
    searchTags: ['product', 'card', 'preview', 'grid', 'list', 'item-card'],
    props: {
        layout: { type: 'enum', options: ['vertical', 'horizontal', 'compact', 'spacious'], required: true },
        enableSwiper: { type: 'boolean', required: true },
        aspectRatio: { type: 'enum', options: ['square', 'portrait', 'landscape'], required: true },
        borderRadius: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'full'], required: true },
        showShadow: { type: 'boolean', required: true },
        hoverZoom: { type: 'boolean', required: true },
        showTitle: { type: 'boolean', required: true },
        titleSize: { type: 'enum', options: ['sm', 'base', 'lg', 'xl', '2xl'], required: true },
        titleWeight: { type: 'enum', options: ['normal', 'medium', 'semibold', 'bold'], required: true },
        titleAlign: { type: 'enum', options: ['left', 'center', 'right'], required: true },
        showPrice: { type: 'boolean', required: true },
        priceSize: { type: 'enum', options: ['sm', 'base', 'lg', 'xl'], required: true },
        priceColor: { type: 'string', required: true },
        showCompareAtPrice: { type: 'boolean', required: true },
        showSavingsBadge: { type: 'boolean', required: true },
        showBadges: { type: 'boolean', required: true },
        showSaleBadge: { type: 'boolean', required: true },
        showNewBadge: { type: 'boolean', required: true },
        showLowStockBadge: { type: 'boolean', required: true },
        badgePosition: { type: 'enum', options: ['top-left', 'top-right', 'bottom-left', 'bottom-right'], required: true },
        showAddToCart: { type: 'boolean', required: true },
        buttonText: { type: 'string', required: true },
        buttonStyle: { type: 'enum', options: ['filled', 'outline', 'ghost'], required: true },
        buttonSize: { type: 'enum', options: ['sm', 'md', 'lg'], required: true },
        showCartIcon: { type: 'boolean', required: true },
        cardRadius: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'xl'], required: true },
        cardBorder: { type: 'enum', options: ['none', 'sm', 'md', 'lg'], required: true },
        cardShadow: { type: 'boolean', required: true },
        cardBackground: { type: 'string', required: true },
        accentColor: { type: 'string', required: true },
        fontFamily: { type: 'string', required: true },
        productId: { type: 'string', required: false },
    },
};
//# sourceMappingURL=productcard.meta.js.map
export const productDescriptionMeta = {
    name: 'ProductDescription',
    label: 'Product Description',
    description: 'Renders product.description (HTML). Configurable font size, text color, line height, max-width (Tailwind prose + narrow), and margin/padding shortcuts. Reads the description from the shared product shape; consumer wrapper injects the product at render-time. Falls back to placeholder text when product is missing.',
    category: 'product',
    intent: ['product', 'description', 'prose'],
    visualRole: 'block',
    dataDeps: ['product (injected)'],
    copyFields: [],
    themeable: ['color'],
    a11yRisk: 'medium',
    a11yNotes: 'Renders HTML via dangerouslySetInnerHTML \u2014 the source (Medusa product description) is trusted in this codebase but new callers should sanitize. Tailwind `prose` class applies sensible default heading/paragraph spacing.',
    mobileBehavior: 'responsive',
    searchTags: ['product', 'description', 'prose', 'long-form'],
    props: {
        fontSize: { type: 'enum', options: ['sm', 'base', 'lg'], required: true },
        color: { type: 'enum', options: ['default', 'gray', 'black'], required: true },
        lineHeight: { type: 'enum', options: ['tight', 'normal', 'relaxed'], required: true },
        maxWidth: { type: 'enum', options: ['full', 'prose', 'narrow'], required: true },
        marginTop: { type: 'string', required: true },
        marginBottom: { type: 'string', required: true },
        paddingX: { type: 'string', required: true },
        paddingY: { type: 'string', required: true },
        product: { type: 'object', required: false },
    },
};
//# sourceMappingURL=productdescription.meta.js.map
export const productVariantSelectorMeta = {
    name: 'ProductVariantSelector',
    label: 'Product Variant Selector',
    description: 'Variant selector with three styles: dropdown, button group, or color swatches. When the user selects options, the matching variant is auto-detected and the active selection is exposed back via the setSelectedVariant callback. Controlled mode: pass selectedOptions + setSelectedOptions + selectedVariant + setSelectedVariant from the cart flow. Dropped an 18-line dev console.log block from the storefront source (was debug noise).',
    category: 'product',
    intent: ['product', 'variant', 'options', 'picker', 'color-swatch'],
    visualRole: 'inline',
    dataDeps: ['product (injected)', 'selectedOptions/setSelectedOptions (cart flow)'],
    copyFields: [],
    themeable: [],
    a11yRisk: 'low',
    a11yNotes: 'Native select for dropdown style (full keyboard a11y). Buttons for buttons + color-swatches styles. Each visible value has an aria-current or visual-selected state via class. Color swatches use title= attribute as accessible label fallback.',
    mobileBehavior: 'responsive',
    searchTags: ['product', 'variant', 'options', 'color', 'size', 'picker'],
    props: {
        selectorStyle: { type: 'enum', options: ['dropdown', 'buttons', 'color-swatches'], required: true },
        showLabels: { type: 'boolean', required: true },
        showStock: { type: 'boolean', required: true },
        marginTop: { type: 'string', required: true },
        marginBottom: { type: 'string', required: true },
        marginLeft: { type: 'string', required: true },
        marginRight: { type: 'string', required: true },
        paddingX: { type: 'string', required: true },
        paddingY: { type: 'string', required: true },
        product: { type: 'object', required: false },
        selectedOptions: { type: 'object', required: false },
        selectedVariant: { type: 'object', required: false },
    },
};
//# sourceMappingURL=productvariantselector.meta.js.map
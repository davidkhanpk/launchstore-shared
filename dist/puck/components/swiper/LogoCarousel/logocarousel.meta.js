export const logoCarouselMeta = {
    name: 'LogoCarousel',
    label: 'Logo Carousel (Swiper)',
    description: 'Auto-scrolling logo strip. Logo URLs (one per line) parsed from textarea field. Optional grayscale with color-on-hover, free mode for smooth scroll. Falls back to 8 mock logos when input is empty. Uses SwiperBase for shared Swiper config.',
    category: 'swiper',
    intent: ['logo', 'brand', 'partner', 'clients'],
    visualRole: 'block',
    dataDeps: [],
    copyFields: ['logoUrls'],
    themeable: ['backgroundColor', 'logoBackgroundColor', 'borderColor'],
    a11yRisk: 'low',
    a11yNotes: 'Each logo image has alt text. Pause-on-hover is enabled by default; consumers can disable if the autoplay feels intrusive to screen-reader users (who can otherwise press Esc to stop).',
    mobileBehavior: 'responsive',
    searchTags: ['logo', 'brand', 'partner', 'clients', 'logo-strip'],
    props: {
        logoUrls: { type: 'string', required: true },
        logosPerView: { type: 'number', required: true },
        logosPerViewTablet: { type: 'number', required: true },
        logosPerViewMobile: { type: 'number', required: true },
        enableAutoplay: { type: 'boolean', required: true },
        autoplaySpeed: { type: 'number', required: true },
        freeMode: { type: 'boolean', required: true },
        loop: { type: 'boolean', required: true },
        spaceBetween: { type: 'number', required: true },
        grayscale: { type: 'boolean', required: true },
        grayscaleHover: { type: 'boolean', required: true },
        logoMaxHeight: { type: 'number', required: true },
        backgroundColor: { type: 'string', required: true },
        logoBackgroundColor: { type: 'string', required: true },
        showBorder: { type: 'boolean', required: true },
        borderColor: { type: 'string', required: true },
        borderRadius: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'xl', 'full'], required: true },
        paddingY: { type: 'number', required: true },
        paddingX: { type: 'number', required: true },
    },
};
//# sourceMappingURL=logocarousel.meta.js.map
export const productCarouselMeta = {
    name: 'ProductCarousel',
    label: 'Product Carousel (Swiper)',
    description: 'Product carousel with optional product-source config (manual/collection/category/featured/bestsellers). Data fetch happens in the consumer wrapper; shared takes `products?: CarouselProduct[]` + optional `onAddToCart(productId)` callback. Defaults render with 0 products (Puck canvas preview). Uses SwiperBase for shared config + side-effect CSS imports.',
    category: 'swiper',
    intent: ['product', 'carousel', 'featured', 'bestsellers', 'related'],
    visualRole: 'block',
    dataDeps: ['products (injected)', 'onAddToCart (injected)'],
    copyFields: ['sectionTitle', 'productIds'],
    themeable: ['backgroundColor', 'cardBackground', 'cardBorderRadius', 'cardShadow'],
    a11yRisk: 'medium',
    a11yNotes: 'Each card is an anchor to /products/handle (full a11y). Add-to-cart button is a real <button> with onClick; consumer should layer aria-label like "Add {title} to cart".',
    mobileBehavior: 'responsive',
    searchTags: ['product', 'carousel', 'featured', 'bestsellers', 'related', 'grid'],
    props: {
        sectionTitle: { type: 'string', required: true },
        showTitle: { type: 'boolean', required: true },
        productSource: { type: 'enum', options: ['manual', 'collection', 'category', 'featured', 'bestsellers'], required: true },
        productIds: { type: 'string', required: true },
        collectionId: { type: 'string', required: true },
        categoryId: { type: 'string', required: true },
        maxProducts: { type: 'number', required: true },
        slidesPerView: { type: 'number', required: true },
        slidesPerViewTablet: { type: 'number', required: true },
        slidesPerViewMobile: { type: 'number', required: true },
        spaceBetween: { type: 'number', required: true },
        effect: { type: 'enum', options: ['slide', 'fade', 'cube', 'coverflow', 'flip'], required: true },
        speed: { type: 'number', required: true },
        navigation: { type: 'boolean', required: true },
        navigationColor: { type: 'string', required: true },
        pagination: { type: 'boolean', required: true },
        paginationType: { type: 'enum', options: ['bullets', 'fraction', 'progressbar'], required: true },
        paginationColor: { type: 'string', required: true },
        autoplay: { type: 'boolean', required: true },
        autoplayDelay: { type: 'number', required: true },
        pauseOnHover: { type: 'boolean', required: true },
        loop: { type: 'boolean', required: true },
        centeredSlides: { type: 'boolean', required: true },
        freeMode: { type: 'boolean', required: true },
        showProductImage: { type: 'boolean', required: true },
        showProductTitle: { type: 'boolean', required: true },
        showProductPrice: { type: 'boolean', required: true },
        showAddToCart: { type: 'boolean', required: true },
        imageAspectRatio: { type: 'enum', options: ['square', 'portrait', 'landscape'], required: true },
        backgroundColor: { type: 'string', required: true },
        cardBackground: { type: 'string', required: true },
        cardBorderRadius: { type: 'enum', options: ['none', 'sm', 'md', 'lg', 'xl'], required: true },
        cardShadow: { type: 'boolean', required: true },
    },
};
//# sourceMappingURL=productcarousel.meta.js.map
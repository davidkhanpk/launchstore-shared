export const categoryProductsGridMeta = { name: 'CategoryProductsGrid', label: 'Category Products Grid', description: 'Category page product grid with title, 5 column options, 4 gap sizes, optional sort+filter bar (uses shared ProductSort), pagination (uses shared Pagination), and per-page dropdown. Cart-library-agnostic: takes products[], totalCount, sortOptions, onSortChange, onPageChange, renderProduct (render-prop escape hatch for consumer-specific product card).', category: 'category', intent: ['category', 'products', 'grid'], visualRole: 'block', dataDeps: ['products (consumer)', 'renderProduct (consumer)'], copyFields: ['title'], themeable: [], a11yRisk: 'medium', a11yNotes: 'Real <a> + <select> + <button>. renderProduct prop allows consumer to inject their own ProductPreview card.', mobileBehavior: 'responsive', searchTags: ['category', 'products', 'grid', 'pagination', 'sort'], props: { showTitle: { type: 'boolean', required: true }, title: { type: 'string', required: true }, titleAlignment: { type: 'enum', options: ['left', 'center', 'right'], required: true }, columns: { type: 'enum', options: ['2', '3', '4', '5', '6'], required: true }, gap: { type: 'enum', options: ['sm', 'md', 'lg', 'xl'], required: true }, gridLocation: { type: 'enum', options: ['left', 'center', 'right'], required: true }, showSortFilter: { type: 'boolean', required: true }, sortPosition: { type: 'enum', options: ['top-left', 'top-center', 'top-right'], required: true }, showPagination: { type: 'boolean', required: true }, paginationPosition: { type: 'enum', options: ['bottom-left', 'bottom-center', 'bottom-right'], required: true }, showProductsPerPageDropdown: { type: 'boolean', required: true }, productsPerPageOptions: { type: 'string', required: true }, productsPerPage: { type: 'number', required: true } } } as const;
export type CategoryProductsGridMeta = typeof categoryProductsGridMeta;
